const Hero = () => {
   return (
      <div>
         <section
            id="home"
            className="w-full flex xl:flex-row flex-col justify-center min-h-screen gap-10 max-container"
         ></section>
      </div>
   );
};

export default Hero;
